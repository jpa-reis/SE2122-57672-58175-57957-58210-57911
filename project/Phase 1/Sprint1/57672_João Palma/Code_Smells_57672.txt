Code Smells:

1 -
scr -> logic -> bibtex -> FieldWriter
Line 62 and 66 - Boolean checks should not be inverted

Solution:
There is no need to invert the result of a boolean comparison.

Use (right != 0) instead of !(right== 0 )


2 -
jabref scr -> logic -> citationkeypattern-> BracketedPattern
Line 387-404 - "switch" statements should have "default" clauses

Solution:
Adding a default clause to the switch allows the program to detect unexpected cases that may 
appear during execution.

...
default:
	error();
	break;
...


3 -
scr -> logic -> exporter -> MSBibExporter
Line 46 - Try-catch blocks should not be nested

Solution:
To avoid making the "Try-catch blocks" nested, the second "try-catch" should be in a separate method.

...
try (AtomicFileWriter ps = new AtomicFileWriter(file, StandardCharsets.UTF_8)) {
            newTryCatch(ps)
        } catch (IOException ex) {
            throw new SaveException(ex);
        }
...

public void newTryCatch(AtomicFileWriter ps){
	try {
		DOMSource source = new DOMSource(msBibDatabase.getDomForExport());
		StreamResult result = new StreamResult(ps);
		Transformer trans = TransformerFactory.newInstance().newTransformer();
		trans.setOutputProperty(OutputKeys.INDENT, "yes");
		trans.transform(source, result);
	} catch (TransformerException | IllegalArgumentException | TransformerFactoryConfigurationError e) {
		throw new SaveException(e);
	}
}
